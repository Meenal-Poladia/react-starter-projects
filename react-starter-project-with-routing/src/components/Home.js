import '../App.css';
import {useHistory, useLocation, useNavigate, useRoutes} from "react-router-dom";

function Home() {

    return (
        <div className="App">
            <h1>We are home</h1>
        </div>
    );
}

export default Home;